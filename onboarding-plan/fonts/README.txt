Place McKinsey Sans Text web font files here for the onboarding blog to use:

- McKinseySansText-Regular.woff2 (and .woff)
- McKinseySansText-Medium.woff2 (and .woff)
- McKinseySansText-SemiBold.woff2 (and .woff)
- McKinseySansText-Bold.woff2 (and .woff)

If your brand toolkit uses different filenames, update the @font-face src URLs in index.html and training-schedule.html to match.

Without these files, the site will use the fallback system font (e.g. Segoe UI on Windows, San Francisco on Mac).
